# DT-001 — Scaffold Next.js Project

| Field | Value |
|-------|-------|
| **Sprint** | 1 — Foundation |
| **Points** | 3 |
| **Status** | 📋 Todo |
| **Assignee** | — |
| **Depends on** | — |
| **Blocks** | DT-002, DT-003, DT-004 |

---

## Description

Initialize the DeutschTutor repository as a Next.js 14 app with TypeScript and the App Router. This is the foundation every other ticket builds on.

---

## Acceptance Criteria

- [ ] `npx create-next-app@latest` run with:
  - TypeScript: **Yes**
  - ESLint: **Yes**
  - App Router: **Yes**
  - Tailwind CSS: **Yes** (handled here, configured in DT-002)
  - `src/` directory: **Yes**
- [ ] Project runs locally with `npm run dev` and shows default Next.js page
- [ ] `.gitignore` includes `node_modules`, `.env.local`, `.next`
- [ ] `README.md` updated with project name and run instructions
- [ ] No TypeScript errors on `npm run build`

---

## Implementation Notes

```bash
npx create-next-app@latest deutsch-tutor \
  --typescript \
  --eslint \
  --tailwind \
  --app \
  --src-dir \
  --import-alias "@/*"
```

Folder structure expected after scaffold:
```
src/
  app/
    layout.tsx
    page.tsx
  components/     ← create this manually
  lib/            ← create this manually
public/
  audio/          ← create this manually (for DT-014)
```

---

## Definition of Done

Code merged to `main`. `npm run dev` shows app. `npm run build` passes with zero errors.
